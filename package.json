{
  "name": "@s6e/tdg",
  "description": "Zero dependencies data generators for tests. Designed to write meaningful data samples and avoid code duplication.",
  "license": "MIT",
  "dependencies": {},
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^20.10.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3",
    "jest": "^29.7.0"
  },
  "scripts": {
    "prepublish": "npm run build",
    "test": "jest",
    "build": "tsc"
  }
}
